# Endereço da API. Em desenvolvimento fica vazio: o Vite faz proxy de /api para
# o backend local, e o painel e a API saem da mesma origem.
#
# Em produção (Vercel), aponte para o domínio do backend no Railway — com o
# caminho /api/v1 no fim. O WebSocket deriva a origem daqui sozinho.
#
# VITE_API_URL=https://api.seu-dominio.com.br/api/v1
